data_vars:
  srf:
    dims: ["w"]
    dtype: "floating"
    attrs:
      units:
        value: "dimensionless"
  srf_u:
    required: false  # does this actually work?
    dims: ["w"]
    dtype: "floating"

coords:
  w:
    dims: ["w"]
    dtype: "floating"
    attrs:
      units:
        value: "nm"  # units_compatible("nm")  # units_equal("nm")  # re("")
